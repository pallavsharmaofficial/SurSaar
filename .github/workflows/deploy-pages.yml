name: Deploy website + web app to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Generate code
        run: |
          dart run build_runner build --delete-conflicting-outputs
          flutter gen-l10n

      - name: Analyze & test
        run: |
          flutter analyze
          flutter test

      - name: Build web app
        # The app lives at /<repo>/app/ under the landing site.
        run: |
          flutter build web --release \
            --base-href "/${{ github.event.repository.name }}/app/" \
            --dart-define=SURSAAR_CONTENT_URL=https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/content/sursaar_content.json

      - name: Assemble site
        run: |
          rm -rf dist
          mkdir -p dist/app dist/content
          cp -R site/. dist/
          cp -R build/web/. dist/app/
          cp content/sursaar_content.json dist/content/
          touch dist/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
